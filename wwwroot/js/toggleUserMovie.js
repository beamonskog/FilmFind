function ToggleFavorite(movieId) {
    //alert("ID= "+ movieId);

    
    //Send post
    //Reload component
}

function ToggleHype(movieId){
    //Send post
    //Reload component
}

function ToggleAdd(movieId) {
    //Send post
    //Reload component
}